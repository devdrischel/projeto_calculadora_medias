const form = document.getElementById('form-atividade');
form.addEventListener('submit', function(e) {
    e.preventDefault();

    const inputNomeAtividade = getElementById('nome-atividade');
    const inputNotaAtividade = getElementById('nota-atividade');

    
});